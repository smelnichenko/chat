server:
  port: 8080

spring:
  application:
    name: chat-service
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:monitor_chat}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "io.schnappy.chat.dto,java.util,java.lang"
        spring.json.value.default.type: "java.util.LinkedHashMap"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true

monitor:
  auth:
    jwt-secret: ${JWT_SECRET}
    token-expiration: ${TOKEN_EXPIRATION:1h}
    secure-cookies: ${SECURE_COOKIES:true}
  chat:
    e2e-enabled: ${CHAT_E2E_ENABLED:false}
    scylla:
      contact-points: ${SCYLLA_HOST:localhost}
      port: ${SCYLLA_PORT:9042}
      datacenter: ${SCYLLA_DATACENTER:datacenter1}
      keyspace: ${SCYLLA_KEYSPACE:chat}
